import React from 'react';
import ItemListContainer from '../item-list';

const HomePage = () => {
  return (
    <React.Fragment>
      <ItemListContainer />
    </React.Fragment>
  );
};

export default HomePage;
